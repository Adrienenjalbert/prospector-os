# Strategic Review — April 2026

> **Pointer.** The full review lives in [`docs/strategic-review-2026-04.md`](../../../docs/strategic-review-2026-04.md)
> (725 lines, anchored to file paths and line ranges). It is the
> canonical authored version.
>
> This `raw/` copy exists so the wiki has a stable reference to ingest
> from. To update: re-read the canonical doc and re-summarise into
> [[strategic-review-2026-04]] (the source page).

---

## What this is

A forensic engineering & product audit of Revenue AI OS, conducted
April 2026. Single audience: founder / engineering lead / RevOps
decision-maker. Method: static review of source under `apps/web`,
`packages/core`, `packages/adapters`, `packages/db`, plus `docs/`.
Every claim anchored to a file path and line range.

## TL;DR — the seven moves that change the curve

| # | Move | Bug-fix | Cost lever | Smart-lever | Effort |
|---|------|---------|------------|-------------|--------|
| 1 | Fix the silently-broken learning loop (exemplar miner mines thumbs-down as positive; slice calibration reads wrong payload key; exemplars never injected; `proposal_type` queries non-existent column) | Yes | Indirect | Yes | S |
| 2 | Implement the prompt optimiser & self-improve LLM steps (`generateObject` with goldens) | No | — | Yes | M |
| 3 | Move `commonBehaviourRules()` (~1.2k tokens) into the cacheable static prefix | No | Yes (10–15%) | — | XS |
| 4 | Replace the hallucinatory `runDeepResearch` cron with a tool-using Sonnet step (web_search / Tavily / Exa) | Yes | Same | Yes (huge) | M |
| 5 | Embed beyond transcripts (companies, signals, conversation notes, exemplars, framework chunks) | No | Yes (lower context) | Yes | M |
| 6 | Unify the two agent paths (Slack route is a parallel runtime) | Yes | Indirect | Yes | M |
| 7 | Honest ROI: implement the holdout-cohort exclusion that's claimed but not in the SQL or insert | Yes | — | — | XS |

If shipped: monthly token spend drops 25–40%, the learning loop
actually closes, ROI becomes defensible, "smart" becomes demonstrable
rather than aspirational.

## What's been shipped since (Phase 1, April 2026)

Per [`CURSOR_PRD.md`](../../../CURSOR_PRD.md) §2:

- Truthfulness gates (holdout exclusion, slice calibration key, action
  panel interaction id, rollback API, hardcoded deep_research disabled)
- Learning loop closure (exemplar miner + injection, prompt-optimizer
  with Opus + `generateObject`, self-improve with Sonnet cluster
  summaries, eval-growth approval flow, retrieval-priors → bandit)
- Zero-config CRM onboarding (≤10 min from CRM connect to cited Slack DM)
- 5 new tier-2 tools, 5 new embedding pipelines
- Slack ↔ dashboard parity (`assembleAgentRun` shared)
- Observable north-star KPIs

The remaining gaps that informed Phase 6 (the Two-Level Second Brain):

1. Embeddings cron does NOT cover `tenant_memories.embedding`.
2. `memory_injected` / `memory_cited` events declared but not emitted.
3. No memory-to-memory graph, no compiled wiki pages, no per-tenant
   schema doc.
4. No consolidation / supersession / decay loops on atoms.

## What I'm using this for in the wiki

This source informs:

- [[learning-loop]] — the closed loop that Phase 1 finally made real.
- [[second-brain]] — the gap analysis that motivated Phase 6.
- [[signal-over-noise]] — the cost-discipline section's empirical numbers.
- [[universal-agent]] — the agent runtime audit (model registry,
  prompt cache, model routing).
